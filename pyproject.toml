[project]
name = "pybamm-study"
version = "0.1.0"
description = "PyBaMM practice scripts"
requires-python = ">=3.11"
dependencies = [
    "pybamm[cite]",
    "matplotlib",
    "ipykernel>=7.2.0",
    "ipywidgets>=8.1.8",
]

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["src"]
